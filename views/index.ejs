<% include partials/header.ejs %>
<% include partials/navigation.ejs %>

  <h1>Bienvenue sur la page <%= description %></h1>
  <p>Les cours d'<%= description %> durent <%= lesson_duration %>
    <% if(lesson_duration == 1) { %>
      heure.
    <% } else { %>
      heures.
    <% } %>
  </p>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>date</th>
        <th>début du cours</th>
        <th>lieu</th>
        <th>places restantes</th>
      </tr>
    </thead>
    <% for(var i=0; i < results.rowCount ; i++) { %>
      <tr>
        <td><%= results.rows[i].lesson_start.toLocaleDateString() %></td>
        <td><%= results.rows[i].lesson_start.toLocaleTimeString() %> </td>
        <td><%= results.rows[i].location %></td>
        <td><%= results.rows[i].remaining_spots %></td>
      </tr>
    <% } %>
  </table>
</div>

<% include partials/footer.ejs %>
